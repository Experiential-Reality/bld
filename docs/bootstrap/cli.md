# CLI

**Cost**: B + D×L = 13 + 5×0.000 = 13.000

---

## Structure

```
structure CLI

B header: title | philosophy | primitives
  title -> emit("bld - BLD Universal Binary (Bootstrap)\n\n")
  philosophy -> emit("BLD: Structure IS Computation\n")
  primitives -> emit("  B (Boundary) - Where does behavior partition?\n"), emit("  L (Link)     - What connects to what?\n"), emit("  D (Dimension)- What repeats?\n\n")
B note: bootstrap_note | usage_intro
  bootstrap_note -> emit("This bootstrap binary was generated by traversing BLD structures.\n")
  usage_intro -> emit("For full functionality, use the Python interpreter:\n\n")
B command: parse | run | compile | cost | markdown
  parse -> usage("bld-py parse <file.bld>"), desc("Parse and display structure")
  run -> usage("bld-py run <file.bld>"), desc("Execute structure")
  compile -> usage("bld-py compile <file.bld>"), desc("Compile to binary")
  cost -> usage("bld-py cost <file.bld>"), desc("Calculate alignment cost")
  markdown -> usage("bld-py markdown <file.bld>"), desc("Emit markdown documentation")
B footer: blank | formulas | url
  blank -> emit("\n")
  formulas -> emit("Cost Formula: Cost = B + D × L\n"), emit("L Formula:    L = -½ ln(1 - ρ²)\n\n")
  url -> emit("https://github.com/experiential-reality-org/bld\n")

L emit_help: header -> note -> command -> footer (deps=0)

D commands: 5 [input, sequential]

returns: output
```

## Boundaries (B)

| Boundary | Partitions | Properties |
|----------|------------|------------|
| header | title \| philosophy \| primitives | topological |
| note | bootstrap_note \| usage_intro | topological |
| command | parse \| run \| compile \| cost \| markdown | topological |
| footer | blank \| formulas \| url | topological |

## Links (L)

| Link | Source | Target | deps | Attributes |
|------|--------|--------|------|------------|
| emit_help | header | note -> command -> footer | 0 |  |

## Dimensions (D)

| Dimension | Extent | Properties |
|-----------|--------|------------|
| commands | 5 | input, sequential |

## Returns

`output`
